# ===========================================
# AutoArt Configuration
# ===========================================
# Copy this file to .env and fill in your values

# Database
DATABASE_URL=postgresql://autoart:password@localhost:5432/autoart
DATABASE_POOL_SIZE=10

# Azure Entra ID (Production - leave empty for development)
# When set, uses Azure AD token auth instead of password
# AZURE_AD_USER=user@yourfirm.com

# JWT Configuration
# Generate a secure secret: openssl rand -base64 64
JWT_SECRET=your-jwt-secret-at-least-32-characters-long
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# CORS (comma-separated for multiple origins)
CORS_ORIGIN=http://localhost:5173

# Frontend (for production)
VITE_API_URL=http://localhost:3001/api

# AutoHelper (Mail module backend)
VITE_AUTOHELPER_URL=http://localhost:8000

# Backup Configuration
BACKUP_DIR=./backups
BACKUP_RETENTION_DAYS=30

# Monday.com OAuth (optional - without these, API key auth is used)
# Create an app at https://developer.monday.com/apps/manage
# MONDAY_CLIENT_ID=your-monday-client-id
# MONDAY_CLIENT_SECRET=your-monday-client-secret
# MONDAY_REDIRECT_URI=http://localhost:3001/api/connections/monday/callback
