
export const MUNICIPALITIES = [];
export const PROCESS_STAGES = [
    { id: 'stage1', shortName: 'Stage 1', requiredDocuments: [] }
];
export const inferStage = (subject: string, body: string) => { void subject; void body; return PROCESS_STAGES[0]; };
export const policyMatrix = {};
